#ifndef COLOR_UTILS_HPP
#define COLOR_UTILS_HPP

#include <string>

	std::string RED(bool isIRC);
	std::string YEL(bool isIRC);
	std::string GRE(bool isIRC);
	std::string BLU(bool isIRC);
	std::string WHI(bool isIRC);
	std::string RESET(bool isIRC);

#endif
